# Sveltia CMS configuration.
#
# LOCAL EDITING: run `npm run dev`, open http://localhost:4321/admin/ and choose
# "Work with Local Repository". Edits write straight to the files in this repo.
# Nothing is uploaded anywhere — you commit and push with Git when you're ready.
#
# The `backend` block below is only used if you ever enable online editing.
# Update `repo` to your GitHub repository before that would work.
backend:
  name: github
  repo: your-username/your-repo
  branch: main

# Where uploaded images are stored, relative to each project's markdown file.
# This resolves to src/assets/uploads and lets Astro optimize the images.
media_folder: "src/assets/uploads"
public_folder: "/src/assets/uploads"

collections:
  - name: projects
    label: Projects
    label_singular: Project
    folder: src/content/projects
    create: true
    slug: "{{slug}}"
    # Per-collection media path (relative to the entry file) so the saved
    # frontmatter path matches what Astro's image() expects.
    media_folder: "../../assets/uploads"
    public_folder: "../../assets/uploads"
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Short description, name: description, widget: text, hint: "One line shown on the homepage card." }
      - { label: Year, name: year, widget: number, value_type: int, min: 1990, max: 2100 }
      - { label: Role, name: role, widget: string, required: false }
      - { label: Client, name: client, widget: string, required: false }
      - { label: Tags, name: tags, widget: list, required: false }
      - { label: Thumbnail, name: thumbnail, widget: image, hint: "Cover image for the homepage grid." }
      - { label: External URL, name: url, widget: string, required: false }
      - { label: Featured, name: featured, widget: boolean, default: false }
      - { label: Draft, name: draft, widget: boolean, default: false }
      - label: Overview (optional)
        name: body
        widget: markdown
        required: false
        hint: "An optional intro paragraph shown right under the title, before the blocks."
      # Ordered, interleaved content. Add a block, choose Text or Gallery,
      # and drag to reorder. Repeat as many times as you like.
      - label: Content blocks
        name: blocks
        widget: list
        required: false
        types:
          - label: Text
            name: text
            widget: object
            summary: "Text — {{fields.heading}}"
            fields:
              - { label: Heading, name: heading, widget: string, required: false }
              - { label: Body, name: body, widget: markdown }
          - label: Gallery
            name: gallery
            widget: object
            summary: "Gallery ({{fields.layout}}, {{fields.images.length}} images)"
            fields:
              - label: Layout
                name: layout
                widget: select
                default: grid-2
                options:
                  - { label: "Two across", value: grid-2 }
                  - { label: "Three across", value: grid-3 }
                  - { label: "Full width", value: full }
              - label: Images
                name: images
                widget: list
                field: { label: Image, name: image, widget: image }
              - { label: Caption, name: caption, widget: string, required: false }
